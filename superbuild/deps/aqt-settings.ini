# One archive at a time. Several workers unpacking into the same Qt prefix race
# on the directories they share, and the path check py7zr runs against a
# directory another worker is creating rejects the file it was about to write.
[aqt]
concurrency : 1
